using UnityEngine;
using System.Collections;

public class TalkerEventTest : MonoBehaviour
{
/// <summary>
/// Raises the enable event.
/// </summary>
	/// 

	/*
	void OnEnable()
	{
		EventManager.onSunUp += DoSomething;
		EventManager.onSunUp += DoSomethingElse;
	}

	void OnDisable()
	{
		EventManager.onSunUp -= DoSomething;
		EventManager.onSunUp -= DoSomethingElse;
	}

	public void DoSomething(float f)
	{
		Debug.Log("Do something." + f);
	}

	public void DoSomethingElse(float f)
	{
		Debug.Log("Do something else." + f);
	}


	void Update()
	{
		if(Input.GetKeyDown(KeyCode.Z))
		{
			EventManager.SunUp(7);
		}
	}
*/
	        
}
